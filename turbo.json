{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "concurrency": "15",
  // Turbo 2 defaults to strict env mode, which hides .env-supplied vars
  // (DATABASE_URL, REDIS_URL, ...) from tasks. Local dev needs them through.
  "envMode": "loose",
  "globalDependencies": ["tsconfig.base.json", "biome.json", ".env", ".env.local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "gen/**"],
      "inputs": [
        "src/**",
        "cad/**",
        "proto/**",
        "buf.yaml",
        "buf.gen.yaml",
        "package.json",
        "tsconfig.json",
        "tsup.config.ts",
        "vite.config.ts",
        "vite.config.*.ts"
      ]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**", "junit.xml"],
      "inputs": ["src/**", "test/**", "vitest.config.ts"]
    },
    "clean": {
      "cache": false
    }
  }
}
