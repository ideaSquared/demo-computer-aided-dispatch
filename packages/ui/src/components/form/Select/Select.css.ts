// Select shares the Input recipe — same chrome, just on a <select> element.

export type { InputVariants as SelectVariants } from '../Input/Input.css.js';
export { input as select } from '../Input/Input.css.js';
