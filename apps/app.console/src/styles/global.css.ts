// Side-effect import of the design-system reset. The theme class is applied
// to the root in `main.tsx` (className, not selector — createTheme returns
// a class name).
import '@cad/lib.ui/styles/reset.css';
